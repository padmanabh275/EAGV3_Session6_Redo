[project]
name = "session6-agent"
version = "0.1.0"
description = "Session 6 cognitive-layer agent with MCP stdio tools."
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.27",
  "mcp>=1.9.0",
  "python-dotenv>=1.0",
  "ddgs>=9.6.0",
  "tavily-python>=0.7.12",
  "crawl4ai>=0.7.6",
  "pydantic>=2.6",
]
